# 首次维护护栏验收

本次是维护基础设施与一处局部排版修复，不是新一轮视觉重构。已确认参照仍为 `4cff5da`；本地改动尚未推送，不将其自行标为新的用户已批准品牌方向。

## 改动与理由

| Before | After |
| --- | --- |
| 品牌决定分散在历史讨论里，README 描述旧网站 | AGENTS、品牌系统、更新流程与当前 README 提供一致的维护入口 |
| 首页主推文案与作品记录分开；精选依赖数组位置 | Session／Experience 自带双语主推文案；首页、Club 与 Studio 使用明确选择；Archive 展示完整记录 |
| 缺少持续验收机制 | 内容来源、资产、双语、实际字体与配色、布局、导航、扫描、搜索信息和浏览器截图进入本地／CI 流程 |
| 主推双栏在 1041px 时只有 360px 最小文字栏，中文短句文字范围略越过栏边 | `globals.css` 的 `.home-feature-layout` 最小文字栏为 380px；不换字体、不改变字重，不裁切文字 |

## 视觉观察

- 主推作品仍由大幅现场封面与唯一标题建立重点；20px 调整只改变窄桌面左右栏的空间分配，没有增加装饰或第二个信息焦点。
- 英文手机标题保留 Playfair 的编辑感，正文与图像之间有清晰停顿；中文主推仍按“录于白昼，／献给入夜之后。”阅读，不把一个短句拆碎。
- 纸色、深色人物区和少量黄色播放入口维持原有节奏。Hao／Leo 的真实人物影像与现场封面仍是主要素材，没有因为建立测试而替换成通用品牌图。

已查看首页桌面整体、EN／ZH 手机主视觉和主推、英文手机 Experience，以及 1041px 修复前后图。自动截图另覆盖两种语言全部六个页面；未宣称逐张人工看完所有截图或完成真实手机验收。

## 本次证据

- `npm run verify`：通过，运行 ID `2026-09-11T21-39-33.314Z`。
- 6 项内容测试、ESLint、TypeScript、隔离生产构建通过。
- 72 项浏览器测试通过；42 项因项目不适用而跳过，例如不在触摸项目重复运行桌面宽度矩阵。
- EN／ZH；320、390、760、1040、1041、1280、1440、1920px；桌面 Chromium、触摸 Chromium、触摸 WebKit。
- 扫描包含桌面指针／拖动／点击复位、手机轻触、Chromium 连续触摸拖动、键盘、一次示范及 reduced motion。真实手机手感与 WebKit 连续拖动仍需设备审阅。
- 本地报告与截图在 `artifacts/harness/`，`run.json` 指向本次运行；旧报告进入时间戳历史目录。证据不进入公共网页。
- `127.0.0.1:3107` 测试服务已退出；未动用户原有预览服务。

CI 文件已写入本地，但未推送、未运行远端 CI，也未修改 GitHub 必需检查或 Vercel 部署门槛。自动通过不等于审美批准；未来新增内容仍须按更新流程留下具体视觉判断。

## 同日补充：历史依据与适用范围

用户提供较完整的历史材料并同意完善 harness 后，本轮仅补维护文档、来源登记与内容检查，没有继续修改页面、样式、素材或公共文案。

- 新增 B01–B05／V01 决策记录：音乐主导、跨文化开放、专业且真实、场景音乐条件、团体与个人署名，以及可演进的视觉基准。区分直接确认、公开呈现、历史方向与维护性推论，不把整理摘要当新 slogan 或执行授权。
- 更新流程分开品牌原则、网站呈现与单次作品条件，加入不同类型更新的人工审阅案例。这些是审阅依据，不是自动语义评分。
- 来源登记新增依据类型、公开状态、适用作品、依据日期与待核对问题；增加反例测试，验证错误类型、非公开状态、错作品、无效日期与媒体链接不匹配确实被拒绝。
- 完整历史未复制到仓库；本机来源索引置于已忽略的 `docs/design/`，且确认未被 Git 跟踪。版本化的维护文档与 harness 未发现本机历史文件路径或原始私聊链接。

本轮 `npm run check` 通过：**7 项内容测试、ESLint、TypeScript**；`git diff --check` 通过。前端差异在本轮前后的 SHA-256 一致，确认没有额外改动 `src/` 或 `public/`。因此本轮未重跑生产构建和浏览器；上面的 72 项浏览器结果与运行 ID 属于先前完整验证，不作为新代码已经跑过浏览器的声明。

内容检查仍输出两条非阻断 **MANUAL REVIEW**：Session 003 的上传／录制／首播日期语义，以及“Dublin's first Yunnan menu”的首份声明。两项均未在本轮独立核实，未擅自改公共文案。未新增历史中发现的作品，也未提交或推送。

## 用户授权推送后的发布前复验

上文的“未推送”记录各阶段当时的状态；用户随后已要求推送 `main`，本段记录提交前的复验，实际推送与部署状态以 Git 和发布回执为准。

- 完整 `npm run verify` 通过，运行 ID `2026-09-11T22-23-52.292Z`：7 项内容测试、ESLint、TypeScript、隔离生产构建、72 项浏览器测试；42 项按项目适用范围跳过。
- 首次受限环境无法下载 Google Fonts；获得联网权限后完整重跑成功，没有换字体或绕过构建检查。
- 再次查看中文 1041px、英文 1440px、两种语言 390px 的主推截图。中文短句完整，图文顺序正确，标题与简介只保留一套，纸色、字体与播放入口维持原有层级。未进行真实手机验收。
- 逐项比对原有中英文公共文案、作品事实、素材链接及人物社媒，迁移前后保持一致。唯一 CSS 改动仍是已记录的窄桌面文字栏修复。
- 暂存范围不含 `docs/design/`、`artifacts/` 或环境文件；两条事实待核实提醒继续保留，未因测试通过而清除。
